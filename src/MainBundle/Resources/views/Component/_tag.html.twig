<ul class="block-tags">
    {% for tag in tags %}
        <li>
            <a class="chip" href="{{ path('tag', {'id' : tag.id, 'name' : tag.name|replace ({' ': '_'})}) }}">
                {{ tag.name }}
            </a>
        </li>
    {% endfor %}
</ul>